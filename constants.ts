import { User, Visit, ClientType, VisitPurpose } from './types';

export const USERS: User[] = [
  { id: 1, username: 'manager', name: 'Sales Manager', role: 'admin', password: 'password' },
];

export const INITIAL_VISITS: Visit[] = [];
